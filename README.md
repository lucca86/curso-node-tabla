# Notas

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es la base de la tabla de multiplicar      [número] [requerido]
  -l, --listar   Indica si se lista o no por consola [booleano] [defecto: false]
  -h, --hasta    Indica la cantidad de iteraciones        [número] [defecto: 10]
```